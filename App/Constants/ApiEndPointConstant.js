import {
  API_URL,
  BOOST_KEYCLOAK_URL,
  CLIENT_ID,
  CLIENT_SECRET
} from '@env';

const EndPoint = {
  API: {
    API_URL,
    BOOST_KEYCLOAK_URL
  },
  SECRET: {
    CLIENT_ID,
    CLIENT_SECRET
  }
};


export { EndPoint };